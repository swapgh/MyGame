package com.game.server.world.components;

/**
 * Player marker component storing the chosen character name.
 *
 * @param characterName the selected character name
 * @since 0.1.0
 */
public record PlayerComponent(String characterName) {
}
