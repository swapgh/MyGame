# Client Skeleton

Short Phase 4 summary.

- Desktop LibGDX launcher added
- `runClient` Gradle task and `tools/run-client.sh` added
- Keyboard-driven login, register, character select, loading, and error screens added
- Auth and world clients connect to the current temporary TCP line protocol
- In-world view is still minimal and uses a placeholder square
